- 2026-04-23: Added enforceable trip/pro chat production-readiness gate (`qa:chat-production-readiness`) backed by `qa/journeys/chat-production-readiness.json` and `scripts/qa/run-chat-production-readiness-gate.cjs` to freeze new chat feature work until token/connect, membership/role, reconnect mutation resilience, and fallback-bound checks all pass in one deterministic command.
- 2026-04-21: Codified concierge transport boundary as SSE/DB-only by deprecating Stream concierge channel factory usage (`getOrCreateConciergeChannel` now throws deterministic unsupported error), removing concierge stream barrel exports/mappers, adding transport-guard regression tests, and documenting architecture in `docs/CONCIERGE_TRANSPORT_BOUNDARY.md`.
- 2026-04-21: Centralized Stream read-state projection by adding `readStateSelectors` (message read-status mapping + unread split helper), switched `TripChat` to selector-driven read status rendering, narrowed `useChatReadReceipts` to debounce/schedule read side-effects only, and expanded Stream unread-count tests for stale windows, invalid timestamps, and sender-self filtering.
- 2026-04-22: Addressed PR #356 review finding where reconnect backfill retry could no-op when pagination was exhausted. `ThreadView` now tracks pagination vs backfill error origin and routes Retry to silent initial backfill fetch for reconnect failures. Added regression test covering reconnect-failure retry path with exhausted pagination state.
- 2026-04-21: Refactored `ThreadView` thread state management to sort replies by `created_at`, dedupe by message id, support Stream `getReplies` pagination via `id_lt`, add initial/pagination retry + loading/error/empty/backfill UI states, and trigger silent thread backfill on Stream reconnect. Added `ThreadView` regression tests for message.new/update/delete, dedupe/order guarantees, pagination error/retry boundaries, and reconnect backfill fetch behavior.
- 2026-04-21: Hardened message edit/delete transport boundaries by adding explicit `transportMode` to `MessageActions` with Stream-only callback enforcement (`onEdit/onDelete` required in stream mode), wiring TripChat + Pro ChannelChatView to pass stream mode callbacks, and keeping legacy DB mutation paths reachable only under explicit legacy mode. Added regression tests to assert Stream mode edit/delete never call `chatService.editChatMessage/deleteChatMessage`.
- 2026-04-21: Added Stream Pro channel backward pagination (`hasMore`, `isLoadingMore`, `loadMore` with `id_lt`) in `useStreamProChannel`, refactored `ChannelChatView` to consume shared stream message/reaction adapter utilities, replaced local Stream optimistic reactions with pending-intent overlay reconciled against server echoes, and added regression tests for Pro pagination plus reaction echo consistency.
- 2026-04-21: Exposed role-gated chat moderation actions in message menus for authorized trip roles (hide message, shadow ban user, mute user, ban user), wired execution through new `stream-moderation-action` edge function with server-side authorization checks, Stream mutations, and `admin_audit_logs` inserts, and added structured telemetry (`moderation_action_executed` / `moderation_action_failed`) plus UI regression tests covering moderation action visibility + invocation.
- 2026-04-17: Fixed Gmail connector initiation failure on `https://chravelapp.com` by adding `chravelapp.com` production origins to edge-function CORS allowlist (`_shared/cors.ts`) and extending CORS security tests to cover those domains.
- 2026-04-16: Fixed Stream chat ReadChannel error surfacing in Messages tab by adding membership self-heal path. Added `stream-ensure-membership` edge function (auth + trip member verification + Stream addMembers) and retry-once flow in `useStreamTripChat` with sanitized fallback messaging.
- 2026-04-16: Fixed TripChat channel-state collision causing MessageTypeBar runtime crash (`channelName.toLowerCase` on Stream channel). Split stream vs role channel state references and hardened MessageTypeBar channel label formatting against undefined names.
- 2026-04-16: Fixed installed-app auth regression that redirected iOS/Android/PWA users to external OAuth browser flows. `useAuth` now hard-blocks Google/Apple OAuth in installed contexts and `AuthModal` hides external OAuth buttons while keeping inline email/password auth available on Home.
- 2026-04-17: Updated web auth modal OAuth layout so Apple and Google sign-in options render side-by-side in browser contexts while installed-app contexts continue to suppress external OAuth buttons.
- 2026-04-16: Hardened Stream chat reliability and UX in TripChat flow: prevented empty re-watch from clobbering hydrated messages, awaited stream-join-channel with timeout, fixed reaction stale-closure handling via refs, enabled thread modal opening via explicit Open thread action with main-timeline parent filtering, removed chat-mode typing gate, and auto-cleared failed-send ghosts on matching Stream message.new echoes.
- 2026-04-16: Fixed installed-app auth regression that redirected iOS/Android/PWA users to external OAuth browser flows. `useAuth` now hard-blocks Google/Apple OAuth in installed contexts and `AuthModal` hides external OAuth buttons while keeping inline email/password auth available on Home.
- 2026-04-16: Fixed thread reply badge action in MessageBubble to open ThreadView via onOpenThread instead of inline-reply handler, aligning visible replyCount CTA with Stream parent_id thread persistence path.
- 2026-04-17: Fixed Stream join timeout surfacing as user-facing chat error (`signal is aborted without reason`) by treating stream-join-channel fetch failures/aborts as best-effort and continuing channel.watch; added regression test covering aborted join fetch path.
- 2026-04-17: Added Stream send fallback for CreateMention permission errors: when mention payload is denied (error code 17), retry message send without `mentioned_users` to prevent false failed-send toasts while preserving message delivery.
- 2026-04-17: Event chat policy simplified to always-open (`everyone`) for all attendees, removed chat-permission controls from Event Admin UI, and added Stream client self-heal connect attempt inside `useStreamTripChat` to avoid event chat timeout when app-level Stream init lags.
- 2026-04-17: Hardened Gmail OAuth connector reliability by preserving same-origin callback context. Frontend now sends origin-derived redirect URI to gmail-auth/connect; edge function validates against allowlist (GOOGLE_REDIRECT_URI + GOOGLE_ADDITIONAL_REDIRECT_URIS), signs redirect URI into OAuth state, and reuses it during token exchange to prevent cross-origin auth/session mismatch failures in preview/local environments.
- 2026-04-18: Updated stream-webhook message.new fanout for trip/broadcast/channel to mention-only recipients from Stream `mentioned_users`, switched notification-center rows to `mention`, and added notification insert dedupe on recipient+type+metadata.stream_message_id to handle webhook retries while preserving concierge/member fanout behavior.
- 2026-04-18: Hardened Stream trip chat membership recovery determinism: structured join failure reasons, validated stream-ensure-membership response before retrying ReadChannel watch, mapped membership failures to actionable UI errors with one guarded reload attempt, standardized stream-join/ensure edge-function error payloads (code/reason), and added regression tests for successful recovery + stable failure error state.
- 2026-04-18: Hardened NotificationsDialog click routing with metadata-first trip resolution (`trip_id`/`trip_type`/`channel_type`), forced mention deep-links to `?tab=chat` across consumer/pro/event routes, and added `chatNavigationContext` route state handshake to support post-navigation chat initialization readiness. Added regression tests for consumer/pro/event mention routing plus missing-metadata fallback safety.
- 2026-04-18: Hardened Stream trip-chat reaction toggling determinism in `useStreamTripChat` by tracking local own-reaction intent across rapid toggles; added regression coverage for add/remove without waiting for reaction events and picker emoji id mapping fallback tests in `MessageReactionBar`.
- 2026-04-19: Added actionable reaction-policy error surfacing in useStreamTripChat toggle path so Stream dashboard misconfiguration/allowlist blocks now show explicit admin guidance to users; covered by new reactions test.
- 2026-04-19: Replaced the "hard-block Google/Apple OAuth in installed contexts" behavior (PRs #309/#311/#320) with an in-app OAuth path that (a) removes the early-return guards in `useAuth.signInWithGoogle`/`signInWithApple`, (b) un-hides the Google/Apple buttons in `AuthModal` for installed contexts, (c) targets `https://chravel.app/auth-callback` Universal Link when installed so the native wrapper can deep-link back into the WebView, and (d) launches the OAuth URL via new `openInstalledAuthBrowser` helper that prefers `window.Capacitor.Plugins.Browser.open` (SFSafariViewController / Chrome Custom Tabs) with `window.location.assign` fallback. Added `/auth-callback` route alias to `AuthPage`. Required prerequisite: native shell (`chravel-mobile`) must register `@capacitor/browser`, the `chravel://` URL scheme, and an `appUrlOpen` listener that routes `chravel.app/auth-callback` deep links back into the WebView — otherwise Google will reject with `disallowed_useragent` in embedded WebView redirects.
- 2026-04-19: Stream webhook hardening follow-up: retired chravel-concierge channel handling, added UUID guards for recipient/trip IDs before notifications upsert, replaced check-then-insert with upsert(ignoreDuplicates) keyed by stream_message_id_mv, added migration for generated-column unique index, updated eventRouting tests, and added local B2 webhook v2 script under scripts/b2-webhook-test/.
- 2026-04-21: Added Stream chat pin/unpin support in message actions for admin/moderator roles, introduced pinned messages filter + inline pinned panel in TripChat, and added regression tests for pin permission visibility plus pinned list consistency helper behavior.
- 2026-04-22: Clarified Broadcasts vs Pinned semantics in MessageTypeBar with active-tab helper hints/tooltips to reduce overlap confusion while keeping both surfaces (option 1), plus regression test coverage for both hint states.
- 2026-04-21: Extended Stream trip/pro chat payload and render mapping to include `quoted_reference` metadata alongside `parent_id`, enabling compact quote blocks (author/time/text) for top-level quoted replies even when parent linkage is unavailable (legacy/non-thread contexts). Added parser round-trip tests for quoted metadata extraction from Stream top-level + `extra_data`.
- 2026-04-21: Enhanced trip/pro chat threading UX and telemetry: parent message thread badges now surface Stream reply_count with inline latest-reply snippet + unread thread indicator, chat search results deep-link thread replies to parent thread open, notification-driven chat deep links can auto-open thread context, and telemetry now tracks thread_opened + thread_reply_sent adoption events.
- 2026-04-21: Audited Stream mention capability path end-to-end: enabled `create-mention` grants for chravel-trip/broadcast/channel in `stream-setup-permissions`, replaced mention send fallback from error-regex-only behavior to explicit channel capability/config checks with guarded fallback retention, and added mention integration tests for send payload behavior plus webhook preference filtering and notification row creation.
- 2026-04-21: Added explicit SupabaseStream role contract validator for pro channels (`roleGrantMembershipContract`), wired validation/reporting into `useRoleAssignments` assign/remove flows, and added contract tests for membership + posting-rights drift on role changes.
- 2026-04-21: Hardened Stream typing indicators by gating legacy TypingIndicatorService fallback to explicit legacy transport mode only, adding throttled outbound Stream keystroke signaling with debounced stopTyping, clearing local typing users on Stream disconnect/channel switch, and adding regression tests for typing start/stop reconnect + transport transitions in useChatTypingIndicators.
- 2026-04-21: Added server-side Stream membership reconciler (stream-reconcile-membership) for chravel-trip + chravel-broadcast validation/repair, introduced canonical reasonCode fields in stream-join-channel and stream-ensure-membership responses, and hardened useStreamTripChat to one deterministic ensure-membership retry with structured telemetry + regression tests for join-timeout recovery, membership denied, and successful deterministic recovery.
- 2026-04-22: PR #354 audit follow-up: fixed stream-reconcile-membership global GET determinism/coverage risk by replacing non-deterministic trip_members limit scan with cursor-based ordered trips batching (`batchSize`/`cursor`/`nextCursor`), capped batch size to bound runtime, and sanitized 500 error responses while preserving server logs for diagnostics.
- 2026-04-21: Extracted Stream message-to-TripChat UI projection into pure adapter (`src/features/chat/adapters/streamMessageViewModel.ts`) and switched `TripChat` to memoized adapter usage so reaction/read-state/attachment/link-preview mapping no longer lives inline in component render logic. Added focused adapter unit coverage for edited messages, attachments/link previews, reaction aggregation, parent reply resolution, and Stream read-state projection.
- 2026-04-21: Hardened trip-cover production parity by adding idempotent migration `20260421171000_enforce_trip_cover_member_permissions.sql` to normalize `trip-covers` bucket/public config and active-member RLS for storage/trips cover writes; unified share links to `buildTripPreviewLink` defaulting to `https://chravel.app` (avoids branded-domain drift), and hardened `api/trip-preview` proxy to force `text/html; charset=utf-8` when upstream body is HTML to prevent raw-source/mojibake desktop previews. Added regression tests for orphan cleanup on DB-write failure, no-row permission failure, and trip-preview content-type coercion.
- 2026-04-21: Added follow-up migration `20260421191000_trip_cover_event_admin_only.sql` to align cover-photo authorization with product intent: consumer/pro trips allow active members, event trips restrict to creator or trip_admins, applied consistently to both storage object policies and `trips.cover_image_url` UPDATE RLS.
- 2026-04-21: Centralized Stream message search into `streamMessageSearch` wrapper used by both trip-level and multi-trip universal search; unified per-channel pagination/error handling + aggregation caps, standardized message/thread deep-link metadata payloads for universal search hits, and added regression tests for empty query guards, disconnected client behavior, and multi-channel aggregation limits.
- 2026-04-23: Added Stream canary guardrails for trusted/internal cohort rollout (`stream_changes_canary` flag + user-hash bucketing), instrumented trip-chat incident reporting for ReadChannel denials/send failures/reconnect backfill failures/mention fallback, and introduced `stream-canary-guard` edge function that auto-disables canary by flipping feature flag off when rolling threshold rates are exceeded.
