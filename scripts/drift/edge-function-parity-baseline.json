{
  "_comment": "Pre-existing, TRACKED edge-function parity drift. These are NOT resolved — they are quarantined so the gate stays green for unrelated PRs while the required supabase/config.toml changes (a protected file) go through a human. Each entry has a follow-up in docs/ACTIVE/drift/DRIFT_AUDIT.md. Do NOT add new entries to silence a failure without reconciling the underlying drift.",
  "knownOrphanedConfigBlocks": [
    "approve-join-request",
    "batch-generate-embeddings",
    "gemini-voice-proxy",
    "gemini-voice-session",
    "livekit-token"
  ],
  "knownPublicWithoutConfig": ["revenuecat-webhook"]
}
